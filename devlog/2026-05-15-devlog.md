# 開發日誌 (2026-05-15)

## 開發規劃 (Planning)
- [x] 確認目前分支位於 `feature/module-leave` 並拉取遠端最新狀態。
- [x] 閱讀知識庫索引、模塊化組件專案頁、AI 交接文件與最新 devlog。
- [x] 追蹤最新 release tag `module-v2026.05.12.1` 的修正內容。
- [x] 更新 AI 規劃入口與 CI 文件檢查中的正式導入基線。
- [x] 補記 `module-report` 不可攜硬依賴問題至 troubleshooting。
- [x] 執行 release / 文件驗證。
- [ ] Commit、push 並觸發 CI。

## 專案結構 (Project Structure)
```text
模塊化組件/
├── .agent/
│   └── workflows/                         # AI 工作流與 Spring 多模組除錯規則
├── .github/
│   └── workflows/
│       └── ci.yml                         # GitHub Actions CI：後端、前端、portable import 與文件檢查
├── devlog/                                # 開發日誌與 troubleshooting
├── docs/
│   ├── module-catalog.md                  # 可匯出模組清冊
│   ├── module-portability-guide.md        # portable bundle 匯出與導入指南
│   └── module-release-guide.md            # release baseline 與升級流程
├── module/
│   ├── backend/                           # Spring Boot multi-module backend
│   ├── docker/local/                      # 本地 Docker Compose 與映像設定
│   ├── env/                               # 環境變數範本
│   └── frontend-web/                      # React/Vite 前端 feature 契約
├── scripts/
│   ├── module-export.sh                   # 模組匯出與 portable bundle 產生工具
│   ├── module-manifest-diff.sh            # manifest 結構化差異比對
│   ├── module-release-check.sh            # release readiness gate
│   └── module-verify-import.sh            # 導入後驗收工具
├── ai-handoff.md                          # AI 接手入口
├── ai-module-change-protocol.md           # 通用修正回寫母體規範
├── ai-module-planning.md                  # 新專案模組規劃入口
├── CHANGELOG.md                           # 變更摘要
├── 企畫書.md
└── 開發與測試整合規劃書.md
```

## 進度 (Progress)
- 已確認工作樹起始狀態乾淨，分支為 `feature/module-leave`，遠端 `origin/feature/module-leave` 已是最新。
- 已確認最新母體 release 為 `module-v2026.05.12.1`，對應 commit `476ae64 fix(report): remove nonportable payroll dependencies`。
- 已閱讀 Obsidian raw 記錄 `2026-05-12-module-report-nonportable-dependencies.md`，確認 report 模組不可攜硬依賴的根因與解法已沉澱。
- 已將 `ai-module-planning.md` 的正式導入基線更新為 `module-v2026.05.12.1`。
- 已同步更新 `.github/workflows/ci.yml` 文件檢查，避免 CI 仍鎖定 `module-v2026.05.10.4`。
- 已在 `CHANGELOG.md` 補上 `module-report` 移除 attendance、finance、payroll 硬依賴的修正摘要。
- 已在 `devlog/troubleshooting.md` 補記 `module-report` 不可攜硬依賴問題，並連回 Obsidian raw 知識庫。

## 驗證 (Verification)
- `bash -n scripts/module-export.sh && bash -n scripts/module-manifest-diff.sh && bash -n scripts/module-release-check.sh && bash -n scripts/module-verify-import.sh`：通過
- `scripts/module-export.sh --all --format markdown > /tmp/module-catalog.md && diff -u docs/module-catalog.md /tmp/module-catalog.md`：通過
- CI 等價文件檢查：通過，確認 `ai-module-planning.md` 與 `.github/workflows/ci.yml` 已更新至 `module-v2026.05.12.1`
- `git diff --check`：通過
- `scripts/module-release-check.sh --modules report --target /tmp/module-release-check-report-20260515 --allow-dirty-source`：通過；母體 source backend tests、frontend audit/lint/build、Docker Compose config 與匯出後 report bundle 驗收皆通過。此為預提交文件修正驗證，manifest 顯示 `dirty=true` 屬預期，不作正式 release tag 使用。

## Git 提交記錄
- 待補。
