# 開發日誌 (2026-04-07)

## 開發規劃 (Planning)
- [x] 初始化 Git 專案並推送到 GitHub
- [x] 建立初始的 `.gitignore`

## 專案結構 (Project Structure)
```
模塊化組件/
├── .git/
├── devlog/
│   ├── 2026-04-06-devlog.md
│   └── 2026-04-07-devlog.md
├── prompt-for-other-ai.md
├── prompt-for-process-planning.md
├── 企畫書.md
├── 開發與測試整合規劃書.md
├── 開發項目模板 母檔/
└── .gitignore
```

## 進度 (Progress)
- 已完成初始化本地 Git Repo
- 新增基礎版 `.gitignore`（過濾 dist, node_modules, 編輯器配置等）
- 將所有既有檔案提交，並綁定遠端倉儲 `git@github.com:billwei1107/module.git`
- 成功推送到 main 分支

## Git 提交記錄
- **提交時間**: 07:47 (依據指令完成時間)
- **提交 Hash**: `6e91a29`
- **提交訊息**: `chore(project): init repository`
  - 專案初始化，提交所有既有檔案與目錄結構
  - 新增基礎 `.gitignore` 檔案
- **變更統計**: 17 個檔案 (+3492/-0)

- **提交時間**: 13:34
- **提交 Hash**: `a6e786f`
- **提交訊息**: `feat(auth): implement authentication module sprint 1-2`
  - 發布 module-auth 模組及資料庫遷移腳本
  - 補足前後端 TypeScript 與 Java JUnit 驗證測試修復
- **變更統計**: 180 個檔案 (+7974/-0)

- **提交時間**: 22:36
- **提交 Hash**: `1574be0`
- **提交訊息**: `fix(core): resolve api double prefix, routing redirect and bean names collision`
  - 修復前端 API `/api/v1` 雙重前綴干擾 Spring Security 驗證問題
  - 修正前端 Axios Interceptor 雙重剝離導致 `data.data` 取值 undefined 報錯
  - 優化 `App.tsx` 萬用路由導向機制，解決首頁被強制踢回 `/login` 的問題
  - 刪去 Auth 模塊中與全局 `SecurityConfig` 同名的 Bean 宣告衝突，落實模塊解耦
- **變更統計**: 39 個檔案 (+228/-404)

---

## 狀態更新：執行 Sprint 1-1 (module-common)

### 開發規劃 (Planning)
- **目標**: 啟動企業模塊化組件系統的第一階段開發，執行 Sprint 1-1 (module-common) 的共用基礎建置。
- **待辦事項**:
  - [x] 拉取專案與確認基礎結構
  - [x] 建立基礎設施配置（Docker & .env）
  - [x] 建立 Spring Boot `pom.xml` 與 `module-common` 後端子模組
  - [x] 實作 `Entity`、`DTO`、`Security` 等共通類
  - [x] 完成 Vite 前端專案 `frontend-web` 並實作 Axios 攔截器與 Zustand Hook
  - [x] 配置並在 Docker Compose 下執行後端微模塊編譯測試與整合準備

### 專案結構 (Project Structure)
目前模塊與系統目錄切分如下 (`module/`):
```text
module/
├── backend/
│   ├── app/                # Spring Boot 啟動入口與 application.yml 組態
│   ├── module-common/      # 後端共用基礎，涵蓋 Auth, JWT, Utils, Feature Toggle
│   └── pom.xml             # Maven 父模塊管理
├── docker/
│   └── local/              # 包含 postgres, redis, backend, frontend 容器配置
├── env/
│   └── local/              # 本地所有的環境變數 (.env)
└── frontend-web/           # React + TS + Vite 前端專案 (含 API Interceptors, store)
```

### 進度 (Progress)
- 已完成 `module-common` 的專案目錄結構設計，所有通用介面（如 `ApiResponse`, `PageResponse`, `BaseEntity`）已實作。
- 已完成全局安全與例外處理（`JwtTokenProvider`, `JwtAuthenticationFilter`, `GlobalExceptionHandler` 等）。
- 完成 Vite 前端專案建立與必要依賴安裝 (`zustand`, `axios`, `@mui/material`)。
- Docker 打包與部署配置就緒，執行容器化編譯驗證中。

---

## 狀態更新：執行 Sprint 1-2 (module-auth)

### 開發規劃 (Planning)
- **目標**: 推進企業模塊化組件系統，執行 Sprint 1-2 (module-auth) 認證與授權模組開發。
- **待辦事項**:
  - [x] 後端 module-auth 子模組建置（含 `pom.xml`, `AuthModuleConfig`）
  - [x] Entity 與資料庫遷移（撰寫 Flyway 腳本，建立 User, Role, Permission 等）
  - [x] Service 業務邏輯實作（AuthService, UserService 等介面及其核心實作）
  - [x] Controller API 與 DTO 實作（AuthController, UserController 等 Rest 端點）
  - [x] 前端 auth 特性實作（LoginPage, UserListPage, RolePermissionTree 等介面）

### 專案結構新增 (Project Structure Updates)
在既有的 `module/` 中擴展如下：
```text
module/
├── backend/
│   ├── app/                
│   ├── module-common/      
│   └── module-auth/        # (新增) 後端認證授權專屬子模組
│       ├── src/main/resources/db/migration/ (Flyway SQL)
│       └── src/main/java... (entity, service, controller, repository, dto)
└── frontend-web/           
    └── src/features/auth/  # (新增) 前端認證授權相關 UI 組件與 API Hook
```

### 進度 (Progress)
- 完成 `module-auth` 資料庫結構與 Entity 模型，支援密碼 BCrypt 加密保存與防範連續登入失敗鎖定。
- 順利建立對應的 DAO (Repositories) 及 Spring Web 控制器 API 介面。
- 建立 Vite 前端的 `LoginPage` 外觀與 Zustand 結合的 `useLogin` API。
- 完成 `UserTable` 與 `RolePermissionTree` 等共用的系統管理介面元件。

---

## 狀態更新：執行 Sprint 1-3 (module-organization)

### 開發規劃 (Planning)
- **目標**: 推進企業模塊化組件系統，執行 Sprint 1-3 (module-organization) 組織架構與員工管理模組。
- **待辦事項**:
  - [x] 後端 module-organization 子模組建置
  - [x] Entity 與資料庫遷移（Company, Department, Employee 等）
  - [x] Service 業務邏輯實作（組織樹查詢、員工自增編號、調職）
  - [x] Controller API 與 DTO 實作
  - [x] 前端 organization 特性實作（DepartmentTree 視圖、EmployeeListPage 等）

### 專案結構新增 (Project Structure Updates)
在既有的 `module/` 中擴展如下：
```text
module/
├── backend/
│   └── module-organization/        # (新增) 後端組織層級子模組
└── frontend-web/           
    └── src/features/organization/  # (新增) 前端部門與員工管理畫面元件
```

### 進度 (Progress)
- 單元測試完成撰寫（驗證了部門樹的層次結構正確性，以及建立員工時是否精準產出對應 EMP 員工編號與派發 Spring 事件）。
- 排除了多模組間相互依賴卻並未完全發布（Install）造成的 Maven 警告與測試失效問題。
- 順利建立對應的 DAO 與前端表格 API。

## Git 提交記錄
- **提交時間**: 目前
- **提交 Hash**: `398a177`
- **提交訊息**: `feat(organization): implement module-organization for enterprise structure and employee management`
  - 新增企業組織與員工管理資料庫模組與資料表結構
  - 實作 Spring Boot 部門自關聯樹狀邏輯與 API 服務層
  - 實作 Vite 前端組織架構頁面與表單介面，並排除 MUI 版本過期警告
- **變更統計**: 66 個檔案變更

---

## 狀態更新：執行 Sprint 1-4 (module-workflow)

### 開發規劃 (Planning)
- **目標**: 推進企業模塊化組件系統，執行 Sprint 1-4 (module-workflow) 建立審批流程引擎後端。
- **待辦事項**:
  - [x] 後端 module-workflow 子模組建置與 Maven 依賴串接
  - [x] 建立 Workflow 相關 Entities 與資料庫遷移 `V1__create_wf_tables.sql`
  - [x] Service 引擎邏輯實作 (`WorkflowEngine.startWorkflow`, `approve`, `reject`, `forward`)，發揮狀態派發 (Spring Event)
  - [x] REST Controller 封裝
  - [x] 前端特性建立 (`workflowApi.ts`, 待辦簽核清單 `MyTasksPage` 視圖)
  - [x] Workflow 流程發起之單元測試 (Mockito + JUnit5)

### 專案結構新增 (Project Structure Updates)
在既有的 `module/` 中再度擴展：
```text
module/
├── backend/
│   └── module-workflow/            # (新增) 主司簽核狀態機流轉引擎叢林
└── frontend-web/           
    └── src/features/workflow/      # (新增) 前台呼叫引擎的簽核審批與代辦元件
```

### 進度 (Progress)
- 修復了 `app` 模組關聯中對應 `module-workflow` 子模組錯誤與 Maven Lombok 的依賴配置異常。
- 單元測試完成度極高，發動 Mockito 驗證引擎發起流程 (`Start`) 是否順利投遞第一站並廣播 `ApprovalPendingEvent`。
- 退件 (`Reject`) 與轉置 (`Forward`) 介面與事件完整補齊，實現去耦合化設計。

## Git 提交記錄
- **提交時間**: 目前
- **提交 Hash**: (待定)
- **提交訊息**: `feat(workflow): implement module-workflow engine and frontend integration`
  - 新增企業工作審批流程之資料庫結構
  - 封裝 `WorkflowEngine` 作為全局事件總管，控制過卡與會簽
  - 實作 Vite 前端發起表單與待辦案件清單視圖
